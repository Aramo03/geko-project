"""
TODO (student wave — models): UserManager for custom User.

- create_user(email, password, **extra_fields)
- create_superuser(email, password, **extra_fields) with role=superuser

Implement after User model exists in models.py.
"""
